import React from 'react';

const Led =(props) => {
    return(
      // <span style={{"fontSize": "24px", "color": this.props.visible ? "#41abe0" : "#999"}}>
      // ●
      // </span>
      <p>led</p>
    )
}

export default Led;